export enum EventName {
  DEPOSIT = 'deposit',
  WITHDRAW = 'withdraw',
  TRANSFER = 'transfer',
  TOPUP = 'topup',
  SETTLEMENT = 'settlement',
}

export enum RPAEventType {
  WITHDRAW = 'withdraw',
  SETTLEMENT = 'settlement',
  COMMISSION = 'commission',
}